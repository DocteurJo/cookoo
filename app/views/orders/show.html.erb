<div class="card text-center">
  <div class="card-header">
    Your order is confirmed!
  </div>
  <div class="card-body justify-content-center">
    <h5 class="card-title">ORDER SUMMARY</h5>
    <p class="card-text">
      <strong>YOUR MEAL OF THE DAY:</strong><br> <%= @order.meal.title %> x <%= @order.meal.quantity %>
    </p>
    <p class="card-text">
      <strong>PICK UP TIME:</strong><br> <%= @order.time %>
    </p>
    <p class="card-text">
      <strong>TOTAL:</strong><br> EUR €<%= @order.meal.quantity *  @order.meal.price %>
    </p>
  <div class="card-footer text-muted">
    Thank you!
  </div>

  <div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-marker="<%= @marker.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
  </div>
</div>
